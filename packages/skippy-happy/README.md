# packages/skippy-happy (v2)

Admiral Living Icon. v1 Soft-FAIL was a pulsing can. v2 is the admiral master with living eyes and character motion.

| Path | Use |
|------|-----|
| `softpass/avatar-idle.gif` | Platform Soft-PASS (`update_state` avatar set) |
| `matrix/{idle,listening,talking}/{mood}.gif` | Full 15-cell matrix |
| `svg/` | Vector twins |

Rebuild: `python3 /workspace/living-icon/_gen/build_skippy_happy.py`
